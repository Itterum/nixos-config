-- Generic physical/virtual display fallback. A future hardware profile can
-- replace this module with explicit outputs, modes, positions, and scales.
hl.monitor({
  output = "",
  mode = "preferred",
  position = "auto",
  scale = 1,
})
